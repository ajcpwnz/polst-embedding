{
  "name": "polst-embedding-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright smoke tests against the deployed polst-embedding GitHub Pages site.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
